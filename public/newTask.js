// listen for new task button being clicked
//frontend - create a new task item which is a form
// include a submit button
// when form is submitted - check for content - allow a blonk form element?
// send post request to server
// server adds new task to the database and returns a response
// if a response is returned - add new task to the frontend ui - replace form with a div element
// form needs to be styled in similar way to the task elements for consisteny

// class system - create a task object which contains functionality 
// task object which others inherit from
// have an array of task objects

// new task add button - append this to the end of each list??

const newTaskBtn = document.querySelectorAll('#createNewTask');

