<% layout('./layouts/boilerplate') -%>

    <div class="row gx-4">
        <% const statuses=['todo', 'doing' , 'done' ]; %>

            <% for (let status of statuses) { %>
                <div class="col-4 px-5 column py-3" id="<%= status %>" data-status="<%= status %>">
                    <div class="p-3 bg-body-secondary rounded bg-opacity-50">
                        <h4 class="fw-light text-dark letter-spacing-loose">
                            <%= status.charAt(0).toUpperCase() + status.slice(1) %>
                        </h4>

                        <% for (let task of tasks) { if (task.status===status) { %>
                            <%- include('./partials/taskCard', { task }) %>
                                <% }} %>
                        <h5 class="fw-light text-dark letter-spacing-tight">
                            + Add a task
                        </h5>
                    </div>
                </div>
                <% } %>
    </div>

    <script src="/app.js"></script>
    <script src="/crud-buttons.js"></script>